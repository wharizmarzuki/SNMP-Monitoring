[pytest]
# Pytest configuration for SNMP Monitoring System

# Test paths
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --asyncio-mode=auto

# Markers
markers =
    unit: Unit tests (isolated service/utility tests)
    integration: Integration tests (API endpoints with database)
    slow: Tests that take a long time to run
    auth: Authentication-related tests
    device: Device endpoint tests
    query: Query endpoint tests
    alert: Alert workflow tests
    snmp: SNMP service tests
    email: Email service tests
    cache: Cache service tests

# Asyncio configuration
asyncio_mode = auto

# Coverage options
[coverage:run]
source = app,services
omit =
    */tests/*
    */venv/*
    */__pycache__/*
    */site-packages/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
